# 番茄钟实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为博客加入全站常驻番茄钟：顶栏胶囊入口（运行时即倒计时显示）+ popover 控制面板，跨软导航/硬刷新存活。

**Architecture:** 纯逻辑状态机放 `src/lib/pomodoro.ts`（vitest 直测，零 DOM）；`Pomodoro.astro` 组件沿用 Search.astro 模式（document 事件委托 + 动态查 DOM + `astro:page-load` 重同步），计时状态存模块作用域（软导航存活）并 write-through 到 localStorage（硬刷新存活），剩余时间永远由 `endsAt - Date.now()` 墙钟重算。

**Tech Stack:** Astro 7 组件内联 TS 脚本、原生 DOM/SGV、vitest、Playwright（既有冒烟脚本）。

**Spec:** `docs/superpowers/specs/2026-09-05-pomodoro-design.md`（本计划从 spec 出发，执行者需同时读 spec）

## Global Constraints

- 零框架：不引入任何 UI 框架、岛屿（is:inline 之外的 `client:*` 一律禁止）、新 npm 依赖
- `src/lib/pomodoro.ts` 是纯逻辑模块：**禁止**导入 `astro:content` 或触碰 DOM
- 软导航铁律：组件脚本不得在模块顶层缓存 DOM 引用（body 每次导航被替换）；事件一律委托到 `document`；初始化模式 = 立即调用 + `document.addEventListener("astro:page-load", init)`
- 时长固定：focus 1500s / short 300s / long 900s；localStorage key `"pomodoro"`，payload 带 `version: 1`
- 配色：专注=`--accent`（青），短休/长休=`--accent-2`（紫）；面板 z-index 90；胶囊移动端断点 `max-width: 767px`
- tick 500ms 且仅在 running 时存在；textContent/标题只在字符串变化时写入
- `prefers-reduced-motion: reduce` 下关闭呼吸/面板/圆环动画
- 门禁顺序：`npm run check` → `npm test` → `npm run build` → `npx serve dist -l 4327` + `python scripts/smoke-test.py`，全绿才提交
- commit message 中文，格式 `类型: 描述`（feat/test/docs/ci/fix）

---

### Task 1: 状态机纯逻辑（TDD）

**Files:**
- Create: `src/lib/pomodoro.ts`
- Test: `src/lib/pomodoro.test.ts`（与源码同目录，站内约定）

**Interfaces:**
- Consumes: 无（第一个任务）
- Produces（Task 2 逐名消费，签名不得偏离）:
  - `type Phase = "focus" | "short" | "long"`
  - `interface PomodoroState { phase: Phase; running: boolean; endsAt: number | null; remainingMs: number; completedFocus: number; awaiting: boolean }`
  - `DURATIONS: Record<Phase, number>`（秒）、`defaultState(): PomodoroState`、`fullMs(phase: Phase): number`
  - `remainingOf(s, now): number`、`advance(s): PomodoroState`、`skip(s): PomodoroState`、`reset(s): PomodoroState`、`start(s, now): PomodoroState`、`pause(s, now): PomodoroState`、`settleExpired(s, now): PomodoroState`
  - `serialize(s): string`、`parse(raw: string | null): PomodoroState`、`formatMs(ms): string`

- [ ] **Step 1: 写失败测试**

创建 `src/lib/pomodoro.test.ts`：

```ts
import { describe, expect, it } from "vitest";
import {
  advance,
  defaultState,
  formatMs,
  fullMs,
  parse,
  pause,
  remainingOf,
  reset,
  serialize,
  settleExpired,
  skip,
  start,
  type PomodoroState,
} from "./pomodoro";

const NOW = 1_700_000_000_000;

describe("advance", () => {
  it("专注结束 → 短休，完成数 +1，待确认不自动开始", () => {
    const s = advance(start(defaultState(), NOW));
    expect(s.phase).toBe("short");
    expect(s.completedFocus).toBe(1);
    expect(s.running).toBe(false);
    expect(s.endsAt).toBeNull();
    expect(s.awaiting).toBe(true);
    expect(s.remainingMs).toBe(fullMs("short"));
  });
  it("第 4 个专注完成 → 长休", () => {
    const s = advance({ ...defaultState(), completedFocus: 3 });
    expect(s.phase).toBe("long");
    expect(s.completedFocus).toBe(4);
  });
  it("休息结束 → 专注，完成数不变", () => {
    const s = advance({ ...defaultState(), phase: "long", completedFocus: 4 });
    expect(s.phase).toBe("focus");
    expect(s.completedFocus).toBe(4);
    expect(s.awaiting).toBe(true);
  });
});

describe("skip / reset", () => {
  it("跳过专注 → 短休但不计完成", () => {
    const s = skip(start(defaultState(), NOW));
    expect(s.phase).toBe("short");
    expect(s.completedFocus).toBe(0);
    expect(s.awaiting).toBe(false);
    expect(s.running).toBe(false);
  });
  it("跳过休息 → 专注", () => {
    const s = skip({ ...defaultState(), phase: "short", completedFocus: 2 });
    expect(s.phase).toBe("focus");
    expect(s.completedFocus).toBe(2);
  });
  it("重置只回当前阶段满时长，完成数不动", () => {
    const s = reset(pause(start(defaultState(), NOW), NOW + 5_000));
    expect(s.remainingMs).toBe(fullMs("focus"));
    expect(s.running).toBe(false);
    expect(s.completedFocus).toBe(0);
  });
});

describe("remainingOf / start / pause", () => {
  it("running 用墙钟差并 clamp 到 0", () => {
    const s = start(defaultState(), NOW);
    expect(remainingOf(s, NOW + 60_000)).toBe(fullMs("focus") - 60_000);
    expect(remainingOf(s, (s.endsAt ?? NOW) + 5_000)).toBe(0);
  });
  it("暂停冻结剩余；再 start 从冻结值续跑", () => {
    const paused = pause(start(defaultState(), NOW), NOW + 5_000);
    expect(paused.running).toBe(false);
    expect(paused.endsAt).toBeNull();
    expect(remainingOf(paused, NOW + 999_000)).toBe(fullMs("focus") - 5_000);
    const resumed = start(paused, NOW + 100_000);
    expect(resumed.endsAt).toBe(NOW + 100_000 + (fullMs("focus") - 5_000));
  });
});

describe("settleExpired", () => {
  it("未过期原样返回", () => {
    const s = start(defaultState(), NOW);
    expect(settleExpired(s, NOW)).toBe(s);
  });
  it("过期（哪怕数小时）只结算一步：专注算完成、待确认", () => {
    const s = settleExpired(start(defaultState(), NOW), NOW + 10 * 3600_000);
    expect(s.phase).toBe("short");
    expect(s.completedFocus).toBe(1);
    expect(s.awaiting).toBe(true);
    expect(s.running).toBe(false);
  });
});

describe("serialize / parse", () => {
  it("roundtrip 保真", () => {
    const s = pause(start({ ...defaultState(), completedFocus: 2 }, NOW), NOW + 30_000);
    expect(parse(serialize(s))).toEqual(s);
  });
  it("损坏 JSON / 版本不符 / 非法阶段 / null → 默认态", () => {
    expect(parse("{oops")).toEqual(defaultState());
    expect(parse('{"version":99}')).toEqual(defaultState());
    expect(parse(serialize(defaultState()).replace('"focus"', '"sleep"'))).toEqual(
      defaultState(),
    );
    expect(parse(null)).toEqual(defaultState());
  });
});

describe("formatMs", () => {
  it("mm:ss 与边界", () => {
    expect(formatMs(1500_000)).toBe("25:00");
    expect(formatMs(300_000)).toBe("05:00");
    expect(formatMs(0)).toBe("00:00");
    expect(formatMs(1)).toBe("00:01"); // ceil 语义：最后 1ms 仍显示 00:01
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/lib/pomodoro.test.ts`
Expected: FAIL——`Cannot find module './pomodoro'`（或等价的模块缺失错误）

- [ ] **Step 3: 最小实现**

创建 `src/lib/pomodoro.ts`：

```ts
// 番茄钟状态机 —— 纯逻辑，零 DOM / 零 astro:content（vitest 直接测）。
// 核心不变量：剩余时间永远由调用方传人的墙钟 now 与 endsAt/remainingMs 推导，
// 不做递减计数器 —— 后台标签页 setInterval 节流到每分钟一次也无损。
export type Phase = "focus" | "short" | "long";

/** 各阶段时长（秒）：专注 25 / 短休 5 / 长休 15 */
export const DURATIONS: Record<Phase, number> = { focus: 1500, short: 300, long: 900 };

export interface PomodoroState {
  phase: Phase;
  running: boolean; // true 时 endsAt 有效
  endsAt: number | null; // epoch ms；非 running 时为 null
  remainingMs: number; // 暂停/待确认时冻结的剩余；running 时忽略
  completedFocus: number; // 自然走完的专注总数（周期圆点取 completedFocus % 4）
  awaiting: boolean; // 阶段自然结束、等待用户确认（呼吸态）
}

export const defaultState = (): PomodoroState => ({
  phase: "focus",
  running: false,
  endsAt: null,
  remainingMs: DURATIONS.focus * 1000,
  completedFocus: 0,
  awaiting: false,
});

export const fullMs = (phase: Phase): number => DURATIONS[phase] * 1000;

/** 剩余毫秒：running 用墙钟差（clamp 0），否则用冻结值 */
export function remainingOf(s: PomodoroState, now: number): number {
  if (s.running && s.endsAt !== null) return Math.max(0, s.endsAt - now);
  return Math.max(0, s.remainingMs);
}

/** 阶段自然结束后的下一阶段：第 4 个专注 → 长休，其余专注 → 短休，休息 → 专注 */
function nextPhase(phase: Phase, completedFocus: number): Phase {
  if (phase === "focus") return (completedFocus + 1) % 4 === 0 ? "long" : "short";
  return "focus";
}

/** 自然结束：完成计数、切换阶段、待确认（决策点：不自动开始下一阶段） */
export function advance(s: PomodoroState): PomodoroState {
  const completedFocus = s.phase === "focus" ? s.completedFocus + 1 : s.completedFocus;
  const phase = nextPhase(s.phase, s.completedFocus);
  return {
    phase,
    running: false,
    endsAt: null,
    remainingMs: fullMs(phase),
    completedFocus,
    awaiting: true,
  };
}

/** 跳过：换阶段但 completedFocus 不变（决策点：跳过 ≠ 完成） */
export function skip(s: PomodoroState): PomodoroState {
  const phase: Phase = s.phase === "focus" ? "short" : "focus";
  return { ...s, phase, running: false, endsAt: null, remainingMs: fullMs(phase), awaiting: false };
}

/** 重置：当前阶段回满时长并暂停；完成计数不动 */
export function reset(s: PomodoroState): PomodoroState {
  return { ...s, running: false, endsAt: null, remainingMs: fullMs(s.phase), awaiting: false };
}

export function start(s: PomodoroState, now: number): PomodoroState {
  const remaining = remainingOf(s, now);
  return { ...s, running: true, endsAt: now + remaining, remainingMs: remaining, awaiting: false };
}

export function pause(s: PomodoroState, now: number): PomodoroState {
  if (!s.running) return s;
  return { ...s, running: false, endsAt: null, remainingMs: remainingOf(s, now) };
}

/** 载入结算：running 且已过期 → 按自然结束结算一步，不连环追账 */
export function settleExpired(s: PomodoroState, now: number): PomodoroState {
  if (s.running && s.endsAt !== null && s.endsAt <= now) return advance(s);
  return s;
}

const STORAGE_VERSION = 1;

export function serialize(s: PomodoroState): string {
  return JSON.stringify({ version: STORAGE_VERSION, ...s });
}

/** 损坏/版本不符/字段非法 → 默认态；任何异常都不抛（隐身模式由调用方 try/catch 兜底） */
export function parse(raw: string | null): PomodoroState {
  const d = defaultState();
  if (!raw) return d;
  try {
    const obj = JSON.parse(raw) as Record<string, unknown>;
    if (obj.version !== STORAGE_VERSION) return d;
    if (obj.phase !== "focus" && obj.phase !== "short" && obj.phase !== "long") return d;
    const phase = obj.phase;
    const endsAt =
      typeof obj.endsAt === "number" && Number.isFinite(obj.endsAt) ? obj.endsAt : null;
    const running = obj.running === true && endsAt !== null;
    const completedFocus =
      typeof obj.completedFocus === "number" && Number.isFinite(obj.completedFocus) && obj.completedFocus >= 0
        ? Math.floor(obj.completedFocus)
        : 0;
    const remainingMs =
      typeof obj.remainingMs === "number" && Number.isFinite(obj.remainingMs) && obj.remainingMs >= 0
        ? obj.remainingMs
        : fullMs(phase);
    return { phase, running, endsAt, remainingMs, completedFocus, awaiting: obj.awaiting === true };
  } catch {
    return d;
  }
}

/** mm:ss（ceil 语义：最后 1ms 显示 00:01，起点整秒显示满时长） */
export function formatMs(ms: number): string {
  const total = Math.ceil(ms / 1000);
  const m = Math.floor(total / 60);
  const sec = total % 60;
  return `${String(m).padStart(2, "0")}:${String(sec).padStart(2, "0")}`;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/lib/pomodoro.test.ts`
Expected: PASS（全部用例绿）

- [ ] **Step 5: 全量门禁中不涉及 UI 的部分**

Run: `npm run check && npm test && npm run build`
Expected: 全绿（本任务只加纯逻辑文件，无 UI 变更，既有冒烟不可能回归）

- [ ] **Step 6: Commit**

```bash
git add src/lib/pomodoro.ts src/lib/pomodoro.test.ts
git commit -m "feat: 番茄钟状态机纯逻辑（推进/跳过/结算/序列化）"
```

---

### Task 2: Pomodoro.astro 组件 + Topbar 挂载

**Files:**
- Create: `src/components/Pomodoro.astro`
- Modify: `src/components/Topbar.astro`（frontmatter import、header 内挂载、`.search-entry` 去 `margin-left: auto`）

**Interfaces:**
- Consumes: Task 1 的全部导出（见 Task 1 Produces，签名逐名一致）
- Produces（Task 3 冒烟依赖的 DOM 契约）:
  - `#pomodoro-pill`（胶囊按钮；`.pomo-time` 子 span 显示 mm:ss）
  - `#pomodoro-panel`（`hidden` 属性开关显隐）
  - `[data-pomo-phase-label]`（面板阶段名文本）、`#pomodoro-panel [data-pomo-time]`（面板大字时间）
  - `[data-pomo-start]` / `[data-pomo-skip]` / `[data-pomo-reset]`（面板三按钮）

- [ ] **Step 1: 创建组件**

创建 `src/components/Pomodoro.astro`，内容如下（完整，勿省略）：

````astro
---
// 番茄钟：顶栏胶囊入口 + 锚定 popover 面板。零岛屿，脚本沿用 Search.astro 模式
// （document 事件委托 + 动态查 DOM + astro:page-load 重同步）。
// 计时状态在模块作用域 —— ClientRouter 不重执行已加载模块，软导航下存活；
// localStorage write-through 让硬刷新/关标签页后按墙钟结算恢复。
---

<div class="pomo-wrap">
  <button
    id="pomodoro-pill"
    type="button"
    aria-haspopup="dialog"
    aria-expanded="false"
    aria-label="番茄钟"
    data-pomodoro-open
  >
    <svg class="pomo-icon" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="12" cy="13" r="8" />
      <path d="M12 9v4l2.5 2.5M9 2h6" />
    </svg>
    <span class="pomo-label">番茄钟</span>
    <span class="pomo-time" hidden>25:00</span>
  </button>

  <div id="pomodoro-panel" class="pomo-panel" hidden role="dialog" aria-label="番茄钟">
    <div class="pomo-ring-row">
      <svg width="140" height="140" viewBox="0 0 140 140" aria-hidden="true">
        <circle class="ring-track" cx="70" cy="70" r="54" />
        <circle class="ring-bar" data-pomo-ring cx="70" cy="70" r="54" />
      </svg>
      <div class="pomo-center">
        <span class="pomo-big" data-pomo-time>25:00</span>
        <span class="pomo-phase" data-pomo-phase-label>专注</span>
      </div>
    </div>
    <div class="pomo-dots" aria-hidden="true">
      <span class="dot" data-pomo-dot></span><span class="dot" data-pomo-dot></span><span class="dot" data-pomo-dot></span><span class="dot" data-pomo-dot></span>
    </div>
    <div class="pomo-actions">
      <button type="button" class="pomo-btn primary" data-pomo-start>开始</button>
      <button type="button" class="pomo-btn" data-pomo-skip>跳过</button>
      <button type="button" class="pomo-btn" data-pomo-reset>重置</button>
    </div>
    <p class="sr-only" aria-live="polite" data-pomo-live></p>
  </div>
</div>

<style>
  /* 胶囊 —— 与 Topbar .search-entry 同款语言；wrap 承担右推 + 面板锚定 */
  .pomo-wrap {
    position: relative;
    margin-left: auto; /* 自身 + 后续搜索胶囊一起推到右侧（搜索的 margin-left:auto 已移除） */
  }
  #pomodoro-pill {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.375rem 0.75rem;
    border: 1px solid var(--border);
    border-radius: 999px;
    background: transparent;
    color: var(--text-3);
    font-size: 0.85rem;
    cursor: pointer;
    white-space: nowrap;
    transition: color var(--t-fast) var(--ease-out), border-color var(--t-fast) var(--ease-out);
  }
  #pomodoro-pill:hover { color: var(--text-2); border-color: var(--border-strong); }
  #pomodoro-pill svg[hidden] { display: none; } /* svg 上 hidden 属性的显式兜底 */
  .pomo-time {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
    min-width: 5ch; /* 闲置 ↔ 计时切换不抖布局 */
  }

  /* 运行态：进度底色填充（--p 由脚本更新），专注=青 / 休息=紫 */
  #pomodoro-pill[data-state="running"] { color: var(--text-1); }
  #pomodoro-pill[data-state="running"][data-phase="focus"] {
    border-color: color-mix(in srgb, var(--accent) 45%, transparent);
    background: linear-gradient(90deg,
      color-mix(in srgb, var(--accent) 20%, transparent) calc(var(--p, 0) * 100%),
      transparent calc(var(--p, 0) * 100%));
  }
  #pomodoro-pill[data-state="running"][data-phase="short"],
  #pomodoro-pill[data-state="running"][data-phase="long"] {
    border-color: color-mix(in srgb, var(--accent-2) 45%, transparent);
    background: linear-gradient(90deg,
      color-mix(in srgb, var(--accent-2) 20%, transparent) calc(var(--p, 0) * 100%),
      transparent calc(var(--p, 0) * 100%));
  }
  #pomodoro-pill[data-state="paused"] { opacity: 0.6; }

  /* 阶段结束待确认：呼吸光晕（无声提示的视觉补强） */
  #pomodoro-pill[data-state="awaiting"] { color: var(--text-1); }
  #pomodoro-pill[data-state="awaiting"][data-phase="focus"] {
    animation: pomo-breathe-cyan 2s var(--ease-out) infinite;
  }
  #pomodoro-pill[data-state="awaiting"][data-phase="short"],
  #pomodoro-pill[data-state="awaiting"][data-phase="long"] {
    animation: pomo-breathe-purple 2s var(--ease-out) infinite;
  }
  @keyframes pomo-breathe-cyan {
    0%, 100% { box-shadow: 0 0 0 0 transparent; }
    50% { box-shadow: 0 0 18px 2px var(--accent-glow); }
  }
  @keyframes pomo-breathe-purple {
    0%, 100% { box-shadow: 0 0 0 0 transparent; }
    50% { box-shadow: 0 0 18px 2px var(--accent-2-glow); }
  }

  /* 面板：锚定胶囊下方。z-90 在 topbar(z-30) 上下文内，搜索 modal(z-100, body 级) 仍盖其上 */
  .pomo-panel {
    position: absolute;
    top: calc(100% + 0.6rem);
    right: 0;
    z-index: 90;
    width: 300px;
    padding: 1.25rem;
    display: grid;
    justify-items: center;
    gap: 0.9rem;
    border: 1px solid var(--border-strong);
    border-radius: var(--radius-lg);
    background: var(--surface);
    box-shadow: 0 24px 80px -20px rgba(0, 0, 0, 0.6), 0 0 60px -30px var(--accent-glow);
    animation: pomo-panel-in 220ms var(--ease-out);
  }
  .pomo-panel[hidden] { display: none; }
  @keyframes pomo-panel-in {
    from { opacity: 0; transform: translateY(8px) scale(0.97); }
  }

  .pomo-ring-row { position: relative; }
  .ring-track, .ring-bar { fill: none; stroke-width: 6; }
  .ring-track { stroke: var(--border); }
  .ring-bar {
    stroke: var(--accent);
    stroke-linecap: round;
    stroke-dasharray: 339.292; /* 2π×54 */
    stroke-dashoffset: 0; /* JS 按 剩余/满时长 更新 */
    transform: rotate(-90deg);
    transform-origin: center;
    transition: stroke-dashoffset 0.4s linear;
  }
  .pomo-panel[data-phase="short"] .ring-bar,
  .pomo-panel[data-phase="long"] .ring-bar { stroke: var(--accent-2); }

  .pomo-center {
    position: absolute;
    inset: 0;
    display: grid;
    place-content: center;
    text-align: center;
    gap: 0.1rem;
  }
  .pomo-big {
    font-family: var(--font-mono);
    font-variant-numeric: tabular-nums;
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-1);
  }
  .pomo-phase { font-size: 0.8rem; color: var(--text-3); letter-spacing: 0.1em; }

  .pomo-dots { display: flex; gap: 0.5rem; }
  .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    border: 1px solid var(--border-strong);
    transition: background-color var(--t-fast) var(--ease-out), border-color var(--t-fast) var(--ease-out);
  }
  .dot.fill { background: var(--accent); border-color: var(--accent); }

  .pomo-actions { display: flex; gap: 0.5rem; }
  .pomo-btn {
    padding: 0.45rem 1rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    background: transparent;
    color: var(--text-2);
    font-size: 0.85rem;
    cursor: pointer;
    transition: color var(--t-fast) var(--ease-out), border-color var(--t-fast) var(--ease-out);
  }
  .pomo-btn:hover { color: var(--text-1); border-color: var(--border-strong); }
  .pomo-btn.primary {
    border-color: color-mix(in srgb, var(--accent) 50%, transparent);
    color: var(--accent);
  }
  .pomo-btn.primary:hover { background: color-mix(in srgb, var(--accent) 10%, transparent); }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
    white-space: nowrap;
    border: 0;
  }

  /* 移动端：胶囊收成纯图标 / 纯时间 */
  @media (max-width: 767px) {
    .pomo-label { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    #pomodoro-pill[data-state="awaiting"] { animation: none; }
    .pomo-panel { animation: none; }
    .ring-bar { transition: none; }
  }
</style>

<script>
  import {
    advance,
    defaultState,
    formatMs,
    fullMs,
    parse,
    pause,
    remainingOf,
    reset,
    serialize,
    settleExpired,
    skip,
    start,
    type Phase,
    type PomodoroState,
  } from "../lib/pomodoro";

  const KEY = "pomodoro";
  const PHASE_LABEL: Record<Phase, string> = { focus: "专注", short: "短休", long: "长休" };
  const RING_C = 2 * Math.PI * 54;

  // 模块作用域状态：软导航下存活（ClientRouter 不重执行已加载模块）。
  // DOM 引用绝不缓存在顶层 —— body 每次导航被替换（Search.astro 已注释的坑）。
  let state: PomodoroState;
  try {
    state = settleExpired(parse(localStorage.getItem(KEY)), Date.now());
  } catch {
    state = defaultState(); // 隐身模式等 localStorage 不可用：纯内存态
  }

  let ticker: ReturnType<typeof setInterval> | null = null;
  let baseTitle = document.title;
  let lastTitle = document.title;

  const getPill = () => document.getElementById("pomodoro-pill");
  const getPanel = () => document.getElementById("pomodoro-panel");

  type PillState = "idle" | "running" | "paused" | "awaiting";
  function pillStateOf(s: PomodoroState): PillState {
    if (s.awaiting) return "awaiting";
    if (s.running) return "running";
    if (s.remainingMs < fullMs(s.phase)) return "paused";
    return "idle";
  }

  function syncTitle(st: PillState, ms: number) {
    let t = baseTitle;
    if (st === "running") t = `${formatMs(ms)} ${PHASE_LABEL[state.phase]} · ${baseTitle}`;
    else if (st === "awaiting") {
      t = `${state.phase === "focus" ? "🍅 该专注了" : "☕ 该休息了"} · ${baseTitle}`;
    }
    if (t !== lastTitle) {
      document.title = t;
      lastTitle = t;
    }
  }

  function renderPanel(ms: number) {
    const panel = getPanel();
    if (!panel) return;
    panel.dataset.phase = state.phase;
    panel.dataset.state = pillStateOf(state);
    const big = panel.querySelector<HTMLElement>("[data-pomo-time]");
    if (big) big.textContent = formatMs(ms);
    const phaseLabel = panel.querySelector<HTMLElement>("[data-pomo-phase-label]");
    if (phaseLabel) phaseLabel.textContent = PHASE_LABEL[state.phase];
    const ring = panel.querySelector<SVGCircleElement>("[data-pomo-ring]");
    if (ring) ring.style.strokeDashoffset = String(RING_C * (ms / fullMs(state.phase)));
    const dots = panel.querySelectorAll<HTMLElement>("[data-pomo-dot]");
    const filled = state.completedFocus % 4;
    dots.forEach((d, i) => d.classList.toggle("fill", i < filled));
    const startBtn = panel.querySelector<HTMLButtonElement>("[data-pomo-start]");
    if (startBtn) startBtn.textContent = state.running ? "暂停" : "开始";
  }

  function render(now = Date.now()) {
    // 自然结束检测在渲染统一结算（tick 或后台唤醒都会走到这里）
    if (state.running && state.endsAt !== null && now >= state.endsAt) {
      const done = state.phase;
      commit(
        advance(state),
        `${PHASE_LABEL[done]}完成，${done === "focus" ? "该休息了" : "该专注了"}`,
      );
      return;
    }
    const pill = getPill();
    if (!pill) return; // 极端时序：新页 DOM 未就绪，下个 tick 自愈
    const ms = remainingOf(state, now);
    const st = pillStateOf(state);
    pill.dataset.state = st;
    pill.dataset.phase = state.phase;
    pill.style.setProperty("--p", String(1 - ms / fullMs(state.phase)));
    const showTime = st !== "idle";
    const icon = pill.querySelector<HTMLElement>(".pomo-icon");
    const label = pill.querySelector<HTMLElement>(".pomo-label");
    const time = pill.querySelector<HTMLElement>(".pomo-time");
    if (icon) icon.hidden = showTime;
    if (label) label.hidden = showTime;
    if (time) {
      time.textContent = formatMs(ms);
      time.hidden = !showTime;
    }
    const panel = getPanel();
    if (panel && !panel.hasAttribute("hidden")) renderPanel(ms);
    syncTitle(st, ms);
  }

  function persist() {
    try {
      localStorage.setItem(KEY, serialize(state));
    } catch {
      /* 隐身模式：跳过持久化，纯内存态 */
    }
  }

  // 唯一常驻开销：running 时 500ms tick；暂停/闲置零定时器
  function ensureTicker() {
    if (state.running && ticker === null) ticker = setInterval(render, 500);
    else if (!state.running && ticker !== null) {
      clearInterval(ticker);
      ticker = null;
    }
  }

  function commit(next: PomodoroState, announce?: string) {
    state = next;
    persist();
    ensureTicker();
    render();
    if (announce) {
      const live = document.querySelector<HTMLElement>("[data-pomo-live]");
      if (live) live.textContent = announce;
    }
  }

  function openPanel() {
    const panel = getPanel();
    if (!panel) return;
    panel.removeAttribute("hidden");
    getPill()?.setAttribute("aria-expanded", "true");
    render();
    panel.querySelector<HTMLButtonElement>("[data-pomo-start]")?.focus();
  }
  function closePanel() {
    getPanel()?.setAttribute("hidden", "");
    const pill = getPill();
    pill?.setAttribute("aria-expanded", "false");
    pill?.focus();
  }

  // —— 事件全部委托到 document：软导航后按钮是新 DOM（同 Search/MenuButton 模式）——
  document.addEventListener("click", (e) => {
    const t = e.target as HTMLElement;
    if (t.closest("[data-pomodoro-open]")) {
      if (getPanel()?.hasAttribute("hidden")) openPanel();
      else closePanel();
      return;
    }
    const panel = getPanel();
    if (!panel || panel.hasAttribute("hidden")) return;
    if (t.closest("[data-pomo-start]")) {
      commit(state.running ? pause(state, Date.now()) : start(state, Date.now()));
      return;
    }
    if (t.closest("[data-pomo-skip]")) {
      commit(skip(state));
      return;
    }
    if (t.closest("[data-pomo-reset]")) {
      commit(reset(state));
      return;
    }
    if (!panel.contains(t)) closePanel(); // 点面板外关闭
  });

  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") {
      const panel = getPanel();
      if (panel && !panel.hasAttribute("hidden")) closePanel();
    }
  });

  // 软导航：标题被新页重置、胶囊是新 DOM —— 重新同步（模块状态不动，计时不断）
  function onPageLoad() {
    baseTitle = document.title;
    lastTitle = document.title;
    render();
    ensureTicker();
  }
  onPageLoad();
  document.addEventListener("astro:page-load", onPageLoad);

  // 后台标签页 setInterval 被节流：回到前台立即按墙钟重算
  document.addEventListener("visibilitychange", () => {
    if (!document.hidden) render();
  });
</script>
````

- [ ] **Step 2: 挂载进 Topbar**

修改 `src/components/Topbar.astro`——共 3 处：

frontmatter 加 import（与现有 import 并列）：

```astro
import Pomodoro from "./Pomodoro.astro";
```

header 内、`<a class="search-entry" ...>` 之前插入：

```astro
  <Pomodoro />
```

`.search-entry` 样式规则删去 `margin-left: auto;` 一行（右推职责移交 `.pomo-wrap`，否则两个 auto margin 会把空闲空间平分、胶囊组不再右贴齐）。改后应为：

```css
  .search-entry {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.375rem 0.75rem;
    border: 1px solid var(--border);
    border-radius: 999px;
    color: var(--text-3);
    font-size: 0.85rem;
    transition: color var(--t-fast) var(--ease-out), border-color var(--t-fast) var(--ease-out);
  }
```

- [ ] **Step 3: 类型检查与构建**

Run: `npm run check && npm run build`
Expected: 全绿（astro check 无新增错误）

- [ ] **Step 4: 既有冒烟回归（UI 变更必须跑）**

Run: `npx serve dist -l 4327`（后台起服务）→ `python scripts/smoke-test.py`
Expected: 既有用例全绿（0 fail）。若 serve 已在跑，直接执行 python 一句。

- [ ] **Step 5: Commit**

```bash
git add src/components/Pomodoro.astro src/components/Topbar.astro
git commit -m "feat: 顶栏番茄钟胶囊与 popover 面板"
```

---

### Task 3: 冒烟用例 + 全量门禁

**Files:**
- Modify: `scripts/smoke-test.py`（第 7.5 节之后、`browser.close()` 之前插入第 8 节）

**Interfaces:**
- Consumes: Task 2 的 DOM 契约（`#pomodoro-pill` / `#pomodoro-panel` / `.pomo-time` / `[data-pomo-time]` / `[data-pomo-phase-label]` / `[data-pomo-start|skip|reset]`）
- Produces: 无（终端任务）

- [ ] **Step 1: 追加冒烟用例**

在 `scripts/smoke-test.py` 的 `mob.close()` 之后、`browser.close()` 之前插入（注意与上文空一行、注释编号顺延为 8）：

```python
    # 8. 番茄钟：胶囊 → 面板 → 开始/暂停/跳过/重置（跳过驱动状态机，不等真实时长）
    page.goto(BASE, wait_until="networkidle")
    check("顶栏番茄钟胶囊存在", page.locator("#pomodoro-pill").count() == 1)
    page.click("#pomodoro-pill")
    check("点击胶囊弹出面板", page.locator("#pomodoro-panel:not([hidden])").count() == 1)
    page.click("[data-pomo-start]")
    page.wait_for_timeout(1300)
    t_run = page.locator("#pomodoro-pill .pomo-time").text_content()
    check("开始后胶囊显示倒计时", t_run == "24:59", f"time={t_run!r}")
    check("运行中标签页标题同步", page.title().startswith("24:59"), f"title={page.title()!r}")
    page.click("[data-pomo-start]")  # 同一按钮切换为暂停
    page.wait_for_timeout(1300)
    t_paused = page.locator("#pomodoro-pill .pomo-time").text_content()
    check("暂停后时间冻结", t_paused == t_run, f"{t_run!r} -> {t_paused!r}")
    page.click("[data-pomo-skip]")  # 跳过专注 → 短休（不计完成，胶囊回闲置态故读面板）
    page.wait_for_timeout(300)
    ph = page.locator("[data-pomo-phase-label]").text_content()
    t_short = page.locator("#pomodoro-panel [data-pomo-time]").text_content()
    check("跳过专注进入短休", ph == "短休" and t_short == "05:00", f"phase={ph!r} time={t_short!r}")
    page.click("[data-pomo-start]")  # 短休开始计时
    page.wait_for_timeout(1300)
    t_going = page.locator("#pomodoro-panel [data-pomo-time]").text_content()
    check("短休计时走到 04:59", t_going == "04:59", f"time={t_going!r}")
    page.click("[data-pomo-reset]")
    page.wait_for_timeout(300)
    t_reset = page.locator("#pomodoro-panel [data-pomo-time]").text_content()
    check("重置回短休满时长", t_reset == "05:00", f"time={t_reset!r}")
    page.keyboard.press("Escape")
    check("Esc 关闭番茄钟面板", page.locator("#pomodoro-panel[hidden]").count() == 1)
```

时序依据：`formatMs` 为 ceil 语义——开始后 1.3s 剩余 1498.7s，显示 24:59（首个显示秒持续到整 2s 边界），断言确定性成立；暂停冻结值仍为 24:59。

- [ ] **Step 2: 全量门禁**

Run: `npm run check && npm test && npm run build && npx serve dist -l 4327`（后台）`&& python scripts/smoke-test.py`
Expected: 全绿——原有用例 + 新增 10 项番茄钟用例 0 fail

- [ ] **Step 3: Commit**

```bash
git add scripts/smoke-test.py
git commit -m "test: 番茄钟冒烟用例（开始/暂停/跳过/重置/标题同步）"
```

- [ ] **Step 4: 收尾核对（对照 spec §8 性能预算）**

人工核对两项（无需写码）：
- Network 面板/构建产物：番茄钟未引入任何新请求与依赖（`dist` 中无新增第三方 chunk）
- DevTools Performance 或直接读码确认：暂停/闲置态无 setInterval（`ticker === null`）

有偏差回到对应任务修正后重跑门禁。
